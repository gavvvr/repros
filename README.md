The "The requested module 'istanbul-lib-coverage' does not provide an export named 'createCoverageMap'" issue
---

See https://github.com/webdriverio/webdriverio/issues/14945

See commits history to understand what is going on.
Use `rm -rf pnpm-lock.yaml node_modules && pnpm i && pnpm run wdio` for testing
